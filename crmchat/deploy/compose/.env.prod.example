# 生产环境模板：生产密钥不入库。
# 上线前复制为部署机 /opt/crm-chat/compose/.env.prod 并补齐全部值
# （部署脚本同步仓库文件时不会覆盖服务器上已存在的 .env.prod）。
APP_KEY=
APP_PORT=20108
APP_DEBUG=false
DB_NAME=crmchat
DB_USER=crmchat
DB_PASSWORD=
DB_PREFIX=eb_
MYSQL_ROOT_PASSWORD=
REDIS_PASSWORD=
