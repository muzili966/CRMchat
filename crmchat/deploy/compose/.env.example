# 复制为部署机 /opt/crm-chat/compose/.env.<env> 并补齐空值
# APP_KEY 为 JWT 签名密钥，生成方式示例：openssl rand -hex 32；一经设置不可变更（会使全部登录态失效）
APP_KEY=
APP_PORT=20118
APP_DEBUG=false
DB_NAME=crmchat
DB_USER=crmchat
DB_PASSWORD=
DB_PREFIX=eb_
MYSQL_ROOT_PASSWORD=
REDIS_PASSWORD=
